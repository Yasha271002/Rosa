<Window x:Class="Rosa.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:Rosa.UI.View"
        xmlns:vm="clr-namespace:Rosa.UI.ViewModel"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        d:DataContext="{d:DesignInstance vm:MainViewModel}">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction
                Command="{Binding LoadedCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:EmployeeViewModel}">
            <view:EmployeeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AccountantViewModel}">
            <view:AccountantView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Horizontal" 
                    Background="#F0F0F0" >
            <TextBlock Text="Тестовый режим (Смена роли):" 
                       VerticalAlignment="Center" 
                       Margin="0 0 15 0"
                       FontWeight="Bold"/>
            <RadioButton Content="Я Сотрудник" 
                         IsChecked="True"
                         Command="{Binding NavigateToEmployeeCommand}" 
                         Margin="0 0 10 0"/>
            <RadioButton Content="Я Бухгалтер" 
                         Command="{Binding NavigateToAccountantCommand}"/>
        </StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" />
    </Grid>
</Window>
