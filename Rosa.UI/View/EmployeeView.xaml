<UserControl x:Class="Rosa.UI.View.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rosa.UI.View"
             xmlns:viewModel="clr-namespace:Rosa.UI.ViewModel"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModel:EmployeeViewModel}">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadMyRequestsCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" 
                BorderBrush="LightGray"
                BorderThickness="1"
                Padding="15" 
                Margin="0,0,10,0" 
                CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Новая заявка"
                           FontSize="18" 
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>

                <TextBlock Text="Сотрудник (введите для смены):" 
                           Margin="0,0,0,5"/>
                <TextBox Text="{Binding CurrentEmployeeName, UpdateSourceTrigger=PropertyChanged, Delay=500}"
                         Background="#FFFFFF"
                         Margin="0 0 0 15"
                         Padding="5"
                         ToolTip="Введите имя другого сотрудника, чтобы загрузить его историю"/>

                <TextBlock Text="Тип справки:" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding CertificateTypes}" 
                          SelectedItem="{Binding SelectedType}" 
                          Margin="0,0,0,15" Padding="5"/>

                <TextBlock Text="Количество экземпляров:" Margin="0 0 0 5"/>
                <TextBox Text="{Binding Quantity, 
                    UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,0,15"
                         Padding="5"/>

                <TextBlock Text="Причина (опционально):" Margin="0 0 0 5"/>
                <TextBox Text="{Binding Reason}" 
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Height="60" 
                         Margin="0 0 0 15"
                         Padding="5"/>

                <Button Content="Отправить заявку" 
                        Command="{Binding SubmitRequestCommand}" 
                        Padding="10" 
                        Background="#007ACC" 
                        Foreground="White"
                        FontWeight="Bold"/>

                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="Red"
                           TextWrapping="Wrap"
                           Margin="0 15 0 0"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1"
                BorderBrush="LightGray"
                BorderThickness="1"
                Padding="15"
                CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           Text="Мои отправленные запросы" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Margin="0 0 0 15"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding MyRequests}" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          CanUserAddRows="False" 
                          AlternatingRowBackground="#F9F9F9">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата" 
                                            Binding="{Binding CreatedAt, StringFormat=\{0:dd.MM.yyyy HH:mm\}}"
                                            Width="120"/>
                        <DataGridTextColumn Header="Тип справки"
                                            Binding="{Binding CertificateType}"
                                            Width="150"/>
                        <DataGridTextColumn Header="Кол-во" 
                                            Binding="{Binding Quantity}" 
                                            Width="60"/>
                        <DataGridTextColumn Header="Статус"
                                            Binding="{Binding Status}"
                                            Width="100"/>
                        <DataGridTextColumn Header="Обновлено"
                                            Binding="{Binding UpdatedAt, StringFormat=\{0:dd.MM.yyyy HH:mm\}}"
                                            Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
