<UserControl x:Class="Rosa.UI.View.AccountantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:Rosa.UI.ViewModel"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModel:AccountantViewModel}">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadAllRequestsCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Панель бухгалтера - Очередь заявок" 
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0 0 0 10"/>

        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   Margin="0 0 0 10"
                   Visibility="{Binding ErrorMessage,
                        Converter={StaticResource StringToVisibilityConverter}}"/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding AllRequests}" 
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" 
                  AlternatingRowBackground="#F9F9F9"
                  RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" 
                                    Binding="{Binding CreatedAt, StringFormat=\{0:dd.MM.yyyy HH:mm\}}" 
                                    Width="120"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Сотрудник" 
                                    Binding="{Binding EmployeeName}"
                                    Width="150"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Вид справки"
                                    Binding="{Binding CertificateType}"
                                    Width="120"
                                    IsReadOnly="True"/>

                <DataGridTextColumn Header="Кол-во"
                                    Binding="{Binding Quantity}"
                                    Width="60" 
                                    IsReadOnly="True"/>

                <DataGridTemplateColumn Header="Причина" 
                                        Width="200"
                                        IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Reason}"
                                       TextWrapping="Wrap" 
                                       ToolTip="{Binding Reason}"
                                       Margin="5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Изменить статус" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.AvailableStatuses, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      SelectedItem="{Binding Status, UpdateSourceTrigger=PropertyChanged}"
                                      Margin="2">
                                <b:Interaction.Triggers>
                                    <b:EventTrigger EventName="SelectionChanged">
                                        <b:InvokeCommandAction 
                                            Command="{Binding DataContext.UpdateRequestStatusCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                    </b:EventTrigger>
                                </b:Interaction.Triggers>
                            </ComboBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
